# Gestor Hospitalario (React)

Proyecto convertido de HTML/JS a React (Vite). Se excluye `Pruevas.html`.

## Cómo correr
1) Instala Node.js 18+.
2) En esta carpeta:
   - `npm install`
   - `npm run dev`

## Datos
Se usan datos demo en `localStorage` (inventario, bitácoras, pendientes, terminados).
